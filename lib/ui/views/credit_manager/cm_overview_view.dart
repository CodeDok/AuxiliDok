import 'package:flutter/material.dart';

class CmOverviewView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Overview of the credit manager')
    );
  }
}